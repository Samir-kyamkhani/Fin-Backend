export interface ICommissionMetadata {
  commissionRate?: number;
  commissionLevel?: number;
  hierarchyPath?: string;
  serviceType?: string;
  provider?: string;
  notes?: string;
  calculatedAt?: Date;
}
