export enum CommissionScope {
  ROLE = 'ROLE',
  USER = 'USER',
}

export enum CommissionType {
  FLAT = 'FLAT',
  PERCENTAGE = 'PERCENTAGE',
}

export enum CreatedByType {
  ROOT = 'ROOT',
  ADMIN = 'ADMIN',
}
