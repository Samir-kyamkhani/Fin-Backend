export enum CommissionScope {
  ROLE = 'ROLE',
  USER = 'USER',
}

export enum CommissionType {
  FLAT = 'FLAT',
  PERCENTAGE = 'PERCENTAGE',
}

export enum CreatorType {
  ROOT = 'ROOT',
  USER = 'USER',
  EMPLOYEE = 'EMPLOYEE',
}
