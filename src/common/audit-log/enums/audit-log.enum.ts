export enum PerformerType {
  ROOT = 'ROOT',
  USER = 'USER',
  EMPLOYEE = 'EMPLOYEE',
  SYSTEM = 'SYSTEM',
}

export enum TargetUserType {
  ROOT = 'ROOT',
  USER = 'USER',
  EMPLOYEE = 'EMPLOYEE',
}

export enum AuditStatus {
  SUCCESS = 'SUCCESS',
  FAILED = 'FAILED',
  PARTIAL = 'PARTIAL',
}
